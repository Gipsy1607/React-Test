create an 3*3 grid in which you move either down or right and you are on the top cell but it will also calculate the paths taken by the us:

function createGrid(rows, columns){
    var grid = [];
    for(i=0, i<=rows.length, i++)
}
createGrid(3,3);


